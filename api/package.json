{
	"name": "api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"types": "wrangler types",
		"w:whoami": "wrangler whoami",
		"w:login": "wrangler login",
		"w:logout": "wrangler logout",
		"d1-init": "wrangler d1 execute DB --file=./script/init.sql",
		"d1-view": "wrangler d1 execute DB --file=./script/view.sql",
		"d1-cmd": "wrangler d1 execute DB --file=./script/cmd.sql",
		"d1-validate-prod": "wrangler d1 execute DB --remote --command 'UPDATE users SET isVerified = 1, accessLevel = 0 WHERE username = \"eleftheros\"';"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.33.0"
	},
	"dependencies": {
		"cookie": "^1.0.2",
		"jose": "^6.1.0"
	}
}
